# Deploy this config on every server (same services/tokens).
# Change bind_addr if servers share a host.
[server]
bind_addr = "0.0.0.0:2333"
default_token = "123"

[server.services.foo1]
bind_addr = "0.0.0.0:80"
